# Can be handy to have a separate config file for the testing folder (the main config file in /main will still trigger first, followed by the one in here for tests)
# This is useful for things like having the test(s) target a different db from the actual production db